Django>=5.1,<5.2
djangorestframework>=3.15,<4.0
django-cors-headers>=4.4,<5.0
django-filter>=24.2,<25.0
drf-spectacular>=0.27,<1.0
django-apscheduler>=0.7,<1.0
APScheduler>=3.10,<4.0
pytz>=2024.1
psycopg2-binary>=2.9,<3.0
pandas>=2.0,<3.0
numpy>=2.0,<3.0

# ---- Celery async task infrastructure ----
celery>=5.3,<6.0
redis>=5.0,<6.0
django-celery-results>=2.5,<3.0

# ---- Raster / geospatial (NC/HDF visualization, optional heavy deps) ----
# 注：仅 ned_vision_plan / data/raster_pipeline 使用；装不上可先注释，
# 不影响数据库、预测流水线与 API 的运行。
xarray>=2024.0
rioxarray>=0.15
rasterio>=1.3
netCDF4>=1.6
pyhdf>=0.11
matplotlib>=3.8
