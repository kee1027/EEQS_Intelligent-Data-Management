# 搭建过程归档（2026-08-25）

> 本文档精简归档本次 Git 双设备生态搭建与前端迁移的全过程，供日后查阅。

## 环境与账号

| 项目 | 值 |
|------|-----|
| 设备 A（主力，Windows） | Git 2.54.0；身份 `shy <she1156547286@163.com>`；SSH 密钥 `~/.ssh/id_ed25519`（ed25519，注释 `-设备A-Windows`），公钥已绑定 GitHub |
| 设备 B | 已按《06-设备B打通清单》接入，可 clone / pull / push |
| GitHub 账号 | `kee1027` |
| 远程仓库 | https://github.com/kee1027/rag-tutorials （**Private**） |
| 本地路径（设备 A） | `C:\Users\Administrator\Documents\Kimi\Workspaces\RAG数据库` |

## 仓库结构（monorepo）

```
rag-tutorials/
├── .gitignore          # 根级：IDE / OS / 环境文件 / 前端排除项
├── README.md
├── frontend/           # EEQS 可视化平台前端（Vue，静态备份用；仅设备 A 跑 npm run dev）
├── backend/            # Django 后端（设备 B 作为后端服务）
├── EEQS智能问答教程/    # 文档
└── Git双设备部署指南/   # 文档
```

## 关键事件时间线

1. **指南编写**：完成《Git双设备部署指南》01–07 共 7 篇（安装 → SSH → 提交 → 双设备同步 → 分支/速查 → 设备B清单 → 手动日常）。
2. **设备 A 打通**：生成 ed25519 密钥对，公钥由用户手动粘贴到 GitHub，`ssh -T` 验证通过。
3. **首次推送**：工作区初始化为仓库，17 文件推送（commit `e20f2f0`、`f27ca55`）。
4. **设备 B 重构**：设备 B 将仓库强推重构为 `frontend/` + `backend/` 单仓（新历史，`66f7b9d`），Django 后端迁入（SQLite + 静态数据模式）。
5. **本地变基**：本地 2 个教程提交 rebase 到远程新历史之上，无数据丢失。
6. **前端迁移**：`EEQS可视化平台\EEQS-frontend` **复制**（非移动，原目录保留）到 `frontend/`，190 文件 / 10543 行（commit `bc7dc05`）。
7. **gitignore 修正**：发现 `frontend/.gitignore` 误忽略 `package-lock.json`，已修复并补提交。

## 迁移时的排除项（已进 .gitignore）

| 排除内容 | 原因 |
|----------|------|
| `node_modules/`、`dist/` | 依赖与构建产物，`npm install` 可恢复 |
| `frontend/*.log`、`frontend/stats.json` | 日志与 44MB 构建分析文件 |
| `frontend/.env.development`、`.env.production` | 环境配置，可能含内网地址/密钥，**不入库**；设备本地各自维护 |

## 运维约定

- **分工**：设备 A = 前端开发（`npm run dev`）；设备 B = 后端服务；`frontend/` 对设备 B 仅作静态备份。
- **同步纪律**：开工 `git pull`，收工 `git push`。
- **设备 A 注意**：前端运行依赖本地的 `frontend/.env.development`（未入库），换机/重装时需手动补回。
